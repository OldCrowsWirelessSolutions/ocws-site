// app/components/StickyQuoteCTA.tsx
"use client";

export default function StickyQuoteCTA() {
  // Disabled to reduce CTA repetition and keep the homepage feeling premium.
  // Re-enable later if we want a softer helper CTA (e.g., “Have questions?”) or only on specific pages.
  return null;
}
