// AUTO-GENERATED from Rookery electron/services/db.ts migrations.

export const MIGRATIONS: string[] = [
  "\n      CREATE TABLE projects (\n        id TEXT PRIMARY KEY,\n        name TEXT NOT NULL,\n        client TEXT,\n        notes TEXT,\n        archived INTEGER NOT NULL DEFAULT 0,\n        created_at INTEGER NOT NULL,\n        updated_at INTEGER NOT NULL\n      );\n\n      CREATE TABLE sites (\n        id TEXT PRIMARY KEY,\n        project_id TEXT NOT NULL REFERENCES projects(id) ON DELETE CASCADE,\n        name TEXT NOT NULL,\n        address TEXT,\n        sqft INTEGER,\n        users INTEGER,\n        density TEXT,\n        notes TEXT,\n        created_at INTEGER NOT NULL,\n        updated_at INTEGER NOT NULL\n      );\n      CREATE INDEX idx_sites_project ON sites(project_id);\n\n      CREATE TABLE verdicts (\n        id TEXT PRIMARY KEY,\n        project_id TEXT NOT NULL REFERENCES projects(id) ON DELETE CASCADE,\n        site_id TEXT REFERENCES sites(id) ON DELETE SET NULL,\n        kind TEXT NOT NULL,\n        title TEXT NOT NULL,\n        body TEXT,\n        findings_count INTEGER NOT NULL DEFAULT 0,\n        severity TEXT,\n        source_kind TEXT NOT NULL,\n        source_path TEXT,\n        created_at INTEGER NOT NULL\n      );\n      CREATE INDEX idx_verdicts_project ON verdicts(project_id);\n      CREATE INDEX idx_verdicts_site ON verdicts(site_id);\n    ",
  "\n      CREATE TABLE reckoning_captures (\n        id TEXT PRIMARY KEY,\n        project_id TEXT NOT NULL REFERENCES projects(id) ON DELETE CASCADE,\n        site_id TEXT REFERENCES sites(id) ON DELETE SET NULL,\n        name TEXT NOT NULL,\n        source_path TEXT,\n        captured_at INTEGER,\n        trail_points INTEGER NOT NULL DEFAULT 0,\n        photo_count INTEGER NOT NULL DEFAULT 0,\n        note_count INTEGER NOT NULL DEFAULT 0,\n        bundle_json TEXT,\n        created_at INTEGER NOT NULL\n      );\n      CREATE INDEX idx_reckoning_project ON reckoning_captures(project_id);\n      CREATE INDEX idx_reckoning_site ON reckoning_captures(site_id);\n    ",
  "\n      CREATE TABLE network_plans (\n        id TEXT PRIMARY KEY,\n        project_id TEXT NOT NULL REFERENCES projects(id) ON DELETE CASCADE,\n        site_id TEXT REFERENCES sites(id) ON DELETE SET NULL,\n        name TEXT NOT NULL,\n        plan_json TEXT NOT NULL,\n        cost_estimate INTEGER,\n        created_at INTEGER NOT NULL,\n        updated_at INTEGER NOT NULL\n      );\n      CREATE INDEX idx_network_plans_project ON network_plans(project_id);\n    ",
  "\n      CREATE TABLE device_builds (\n        id TEXT PRIMARY KEY,\n        project_id TEXT NOT NULL REFERENCES projects(id) ON DELETE CASCADE,\n        name TEXT NOT NULL,\n        sheet_json TEXT NOT NULL,\n        created_at INTEGER NOT NULL,\n        updated_at INTEGER NOT NULL\n      );\n      CREATE INDEX idx_device_builds_project ON device_builds(project_id);\n    ",
  "\n      -- Subjects taxonomy. Reference data; seeded immediately below.\n      CREATE TABLE subjects (\n        id TEXT PRIMARY KEY,\n        slug TEXT UNIQUE NOT NULL,\n        name TEXT NOT NULL,\n        description TEXT,\n        parent_subject_id TEXT REFERENCES subjects(id),\n        grade_band_min TEXT,\n        grade_band_max TEXT,\n        domain_tags_json TEXT,\n        standards_alignment_json TEXT\n      );\n      CREATE INDEX idx_subjects_parent ON subjects(parent_subject_id);\n\n      -- Institutions (Campus only). Self-referential for district→school /\n      -- system→campus nesting.\n      CREATE TABLE institutions (\n        id TEXT PRIMARY KEY,\n        name TEXT NOT NULL,\n        type TEXT NOT NULL CHECK (type IN ('k12_school','k12_district','higher_ed_campus','higher_ed_system')),\n        parent_institution_id TEXT REFERENCES institutions(id),\n        hierarchy_path TEXT,\n        contact_email TEXT,\n        contact_name TEXT,\n        sso_provider TEXT,\n        sso_config_json TEXT,\n        sis_provider TEXT,\n        sis_config_json TEXT,\n        standards_alignment_default TEXT,\n        state_code TEXT,\n        active INTEGER NOT NULL DEFAULT 1,\n        created_at INTEGER NOT NULL\n      );\n      CREATE INDEX idx_institutions_parent ON institutions(parent_institution_id);\n\n      -- Supervisors (parents for Aerie; instructors / admins for Campus).\n      -- 'role' values are surface-specific but kept in one column for\n      -- single-table-of-people simplicity.\n      CREATE TABLE supervisors (\n        id TEXT PRIMARY KEY,\n        surface TEXT NOT NULL CHECK (surface IN ('aerie','campus_k12','campus_higher_ed')),\n        role TEXT NOT NULL CHECK (role IN ('parent','teacher','instructor','department_admin','campus_admin','system_admin','district_admin')),\n        email TEXT NOT NULL,\n        first_name TEXT,\n        last_name TEXT,\n        institution_id TEXT REFERENCES institutions(id) ON DELETE SET NULL,\n        hierarchy_scope_json TEXT,\n        active INTEGER NOT NULL DEFAULT 1,\n        created_at INTEGER NOT NULL\n      );\n      CREATE INDEX idx_supervisors_institution ON supervisors(institution_id);\n\n      -- Learners. Universal across Aerie / Academy / Campus.\n      -- sex is stored for non-UX use only (e.g., TTS voice register through\n      -- puberty); the renderer must not branch UX on it. Per cognitive-UX\n      -- research: cognitive_profile_json carries a two-layer structure:\n      --   { diagnostic_hints: { adhd, gifted, dyslexia, asd, twice_exceptional, ... },\n      --     preferences: { tts_default_on, reduced_motion, low_stim_mode,\n      --                    high_contrast_mode, session_minutes_target,\n      --                    movement_break_prompts, gamification: {streaks,\n      --                    leaderboard, badges}, hands_on_first, ... } }\n      -- mentor_bird_pref picks the corvid voice (Corvus / Mira / Pip / Sage).\n      CREATE TABLE learners (\n        id TEXT PRIMARY KEY,\n        surface TEXT NOT NULL CHECK (surface IN ('aerie','academy','campus_k12','campus_higher_ed')),\n        parent_account_id TEXT,\n        institution_id TEXT REFERENCES institutions(id) ON DELETE SET NULL,\n        first_name TEXT NOT NULL,\n        preferred_name TEXT,\n        last_name TEXT,\n        birthdate TEXT,\n        age INTEGER,\n        default_grade_level TEXT,\n        sex TEXT CHECK (sex IN ('male','female')),\n        cognitive_profile_json TEXT,\n        per_subject_grade_override_json TEXT,\n        goals_json TEXT,\n        iep_504_flags_json TEXT,\n        major TEXT,\n        department TEXT,\n        enrolled_courses_json TEXT,\n        email TEXT,\n        external_id TEXT,\n        mentor_bird_pref TEXT CHECK (mentor_bird_pref IN ('corvus','mira','pip','sage')),\n        interests_structured_json TEXT,\n        ux_band_override TEXT CHECK (ux_band_override IN ('K-2','3-5','6-8','9-12','adult')),\n        active INTEGER NOT NULL DEFAULT 1,\n        archived INTEGER NOT NULL DEFAULT 0,\n        created_at INTEGER NOT NULL,\n        updated_at INTEGER NOT NULL\n      );\n      CREATE INDEX idx_learners_surface ON learners(surface);\n      CREATE INDEX idx_learners_parent ON learners(parent_account_id);\n      CREATE INDEX idx_learners_institution ON learners(institution_id);\n\n      -- Aerie family accounts (parent + kids structure).\n      -- Forward-references supervisors which is created above. subscription_tier\n      -- carries Aerie-specific values local to this column (NOT joined to the\n      -- global Tier union in entitlement.ts; that expansion is Phase F).\n      CREATE TABLE aerie_families (\n        id TEXT PRIMARY KEY,\n        primary_supervisor_id TEXT NOT NULL REFERENCES supervisors(id),\n        family_name TEXT,\n        subscription_tier TEXT NOT NULL CHECK (subscription_tier IN ('aerie','aerie_scholar')),\n        seat_count_kids INTEGER NOT NULL DEFAULT 3,\n        seat_count_used INTEGER NOT NULL DEFAULT 0,\n        ocws_subscription_id TEXT,\n        created_at INTEGER NOT NULL\n      );\n      CREATE INDEX idx_aerie_families_supervisor ON aerie_families(primary_supervisor_id);\n\n      -- Supervisor ↔ learner join.\n      CREATE TABLE supervisor_learners (\n        supervisor_id TEXT NOT NULL REFERENCES supervisors(id) ON DELETE CASCADE,\n        learner_id TEXT NOT NULL REFERENCES learners(id) ON DELETE CASCADE,\n        relationship TEXT,\n        PRIMARY KEY (supervisor_id, learner_id)\n      );\n      CREATE INDEX idx_supervisor_learners_learner ON supervisor_learners(learner_id);\n\n      -- Institutional courses. Higher-ed-shaped; K-12 maps loosely.\n      CREATE TABLE institutional_courses (\n        id TEXT PRIMARY KEY,\n        institution_id TEXT NOT NULL REFERENCES institutions(id) ON DELETE CASCADE,\n        external_course_id TEXT,\n        course_code TEXT,\n        title TEXT NOT NULL,\n        description TEXT,\n        subject_id TEXT REFERENCES subjects(id) ON DELETE SET NULL,\n        instructor_supervisor_id TEXT REFERENCES supervisors(id) ON DELETE SET NULL,\n        term TEXT,\n        standards_alignment_json TEXT,\n        active INTEGER NOT NULL DEFAULT 1\n      );\n      CREATE INDEX idx_inst_courses_institution ON institutional_courses(institution_id);\n      CREATE INDEX idx_inst_courses_subject ON institutional_courses(subject_id);\n\n      -- Persistent Corvus memory per learner. The \"make Corvus feel real\"\n      -- mechanic. memory_type is a controlled vocabulary; weight decays.\n      CREATE TABLE learner_memory (\n        id TEXT PRIMARY KEY,\n        learner_id TEXT NOT NULL REFERENCES learners(id) ON DELETE CASCADE,\n        memory_type TEXT NOT NULL CHECK (memory_type IN (\n          'topic_covered','topic_mastered','difficulty_observed','preference','interest',\n          'project_history','reference_anchor','sibling_collaboration','goal_progress',\n          'session_summary','corvus_observation'\n        )),\n        subject TEXT,\n        content TEXT NOT NULL,\n        weight REAL NOT NULL DEFAULT 1.0,\n        created_at INTEGER NOT NULL,\n        last_referenced_at INTEGER\n      );\n      CREATE INDEX idx_memory_learner ON learner_memory(learner_id);\n      CREATE INDEX idx_memory_type ON learner_memory(learner_id, memory_type);\n\n      -- Lessons. Corvus-generated structured units of instruction.\n      CREATE TABLE lessons (\n        id TEXT PRIMARY KEY,\n        learner_id TEXT NOT NULL REFERENCES learners(id) ON DELETE CASCADE,\n        subject_id TEXT REFERENCES subjects(id) ON DELETE SET NULL,\n        grade_level_used TEXT NOT NULL,\n        title TEXT NOT NULL,\n        objectives_json TEXT,\n        body_markdown TEXT NOT NULL,\n        resources_json TEXT,\n        estimated_duration_minutes INTEGER,\n        status TEXT NOT NULL CHECK (status IN ('assigned','in_progress','completed','skipped')) DEFAULT 'assigned',\n        generated_at INTEGER NOT NULL,\n        started_at INTEGER,\n        completed_at INTEGER,\n        time_spent_seconds INTEGER NOT NULL DEFAULT 0,\n        parent_lesson_id TEXT REFERENCES lessons(id) ON DELETE SET NULL,\n        notes_for_supervisor TEXT\n      );\n      CREATE INDEX idx_lessons_learner ON lessons(learner_id);\n      CREATE INDEX idx_lessons_status ON lessons(learner_id, status);\n\n      -- Learner projects (renamed from prompt's \"projects\" to avoid the\n      -- existing engineering-projects table). Hands-on tasks producing\n      -- artifacts. Cross-sibling collaboration tracked via the join table\n      -- sibling_collaborations below; collaborative_with_learner_id is also\n      -- carried for the simple two-learner case.\n      CREATE TABLE learner_projects (\n        id TEXT PRIMARY KEY,\n        learner_id TEXT NOT NULL REFERENCES learners(id) ON DELETE CASCADE,\n        lesson_id TEXT REFERENCES lessons(id) ON DELETE SET NULL,\n        collaborative_with_learner_id TEXT REFERENCES learners(id) ON DELETE SET NULL,\n        title TEXT NOT NULL,\n        description_markdown TEXT NOT NULL,\n        rubric_json TEXT NOT NULL,\n        materials_json TEXT,\n        steps_json TEXT,\n        status TEXT NOT NULL CHECK (status IN ('assigned','in_progress','submitted','graded','archived')) DEFAULT 'assigned',\n        generated_at INTEGER NOT NULL,\n        started_at INTEGER,\n        submitted_at INTEGER,\n        graded_at INTEGER,\n        time_spent_seconds INTEGER NOT NULL DEFAULT 0,\n        artifact_paths_json TEXT,\n        reflection_text TEXT,\n        grade_letter TEXT,\n        grade_numeric REAL,\n        corvus_feedback_markdown TEXT\n      );\n      CREATE INDEX idx_learner_projects_learner ON learner_projects(learner_id);\n      CREATE INDEX idx_learner_projects_collab ON learner_projects(collaborative_with_learner_id);\n\n      -- Assessments. Quizzes AND tests in one table, distinguished by 'type'.\n      CREATE TABLE assessments (\n        id TEXT PRIMARY KEY,\n        learner_id TEXT NOT NULL REFERENCES learners(id) ON DELETE CASCADE,\n        lesson_id TEXT REFERENCES lessons(id) ON DELETE SET NULL,\n        type TEXT NOT NULL CHECK (type IN ('quiz','test','midterm','final','placement','practice_certification')),\n        title TEXT NOT NULL,\n        questions_json TEXT NOT NULL,\n        responses_json TEXT,\n        score_numeric REAL,\n        score_percent REAL,\n        score_letter TEXT,\n        status TEXT NOT NULL CHECK (status IN ('assigned','in_progress','submitted','graded')) DEFAULT 'assigned',\n        generated_at INTEGER NOT NULL,\n        started_at INTEGER,\n        submitted_at INTEGER,\n        graded_at INTEGER,\n        time_spent_seconds INTEGER NOT NULL DEFAULT 0,\n        corvus_feedback_markdown TEXT\n      );\n      CREATE INDEX idx_assessments_learner ON assessments(learner_id);\n\n      -- Learning sessions. Time-tracking source of truth. tokens_used is\n      -- carried from the start so cost analytics can be added later without\n      -- a follow-up migration.\n      CREATE TABLE learning_sessions (\n        id TEXT PRIMARY KEY,\n        learner_id TEXT NOT NULL REFERENCES learners(id) ON DELETE CASCADE,\n        surface_type TEXT NOT NULL CHECK (surface_type IN ('lesson','project','assessment','free_chat','review')),\n        reference_id TEXT,\n        started_at INTEGER NOT NULL,\n        ended_at INTEGER,\n        duration_seconds INTEGER,\n        transcript_json TEXT,\n        summary_text TEXT,\n        tokens_used INTEGER NOT NULL DEFAULT 0\n      );\n      CREATE INDEX idx_sessions_learner ON learning_sessions(learner_id);\n      CREATE INDEX idx_sessions_dates ON learning_sessions(learner_id, started_at);\n\n      -- Subject-completion certificates issued by Corvus. PDF generation\n      -- happens in the export pipeline (Phase F); this table holds the\n      -- metadata + path.\n      CREATE TABLE certificates (\n        id TEXT PRIMARY KEY,\n        learner_id TEXT NOT NULL REFERENCES learners(id) ON DELETE CASCADE,\n        subject_id TEXT REFERENCES subjects(id) ON DELETE SET NULL,\n        title TEXT NOT NULL,\n        issued_at INTEGER NOT NULL,\n        pdf_path TEXT,\n        hours_logged INTEGER,\n        lessons_completed INTEGER,\n        projects_completed INTEGER,\n        assessments_passed INTEGER,\n        watermark TEXT,\n        certificate_data_json TEXT\n      );\n      CREATE INDEX idx_certificates_learner ON certificates(learner_id);\n\n      -- Cross-sibling collaboration tracking. Distinct from the simple\n      -- two-learner case on learner_projects — this table can carry richer\n      -- multi-sibling structures and per-sibling roles.\n      CREATE TABLE sibling_collaborations (\n        id TEXT PRIMARY KEY,\n        project_id TEXT NOT NULL REFERENCES learner_projects(id) ON DELETE CASCADE,\n        primary_learner_id TEXT NOT NULL REFERENCES learners(id) ON DELETE CASCADE,\n        collaborator_learner_id TEXT NOT NULL REFERENCES learners(id) ON DELETE CASCADE,\n        primary_role TEXT,\n        collaborator_role TEXT,\n        created_at INTEGER NOT NULL\n      );\n      CREATE INDEX idx_sibling_collab_project ON sibling_collaborations(project_id);\n    ",
  "\n      CREATE TABLE ferpa_audit_log (\n        id TEXT PRIMARY KEY,\n        institution_id TEXT REFERENCES institutions(id) ON DELETE SET NULL,\n        supervisor_id TEXT REFERENCES supervisors(id) ON DELETE SET NULL,\n        learner_id TEXT REFERENCES learners(id) ON DELETE SET NULL,\n        access_type TEXT NOT NULL,\n        details TEXT,\n        accessed_at INTEGER NOT NULL\n      );\n      CREATE INDEX idx_ferpa_institution ON ferpa_audit_log(institution_id);\n      CREATE INDEX idx_ferpa_learner ON ferpa_audit_log(learner_id);\n      CREATE INDEX idx_ferpa_accessed_at ON ferpa_audit_log(accessed_at);\n\n      CREATE TABLE campus_import_runs (\n        id TEXT PRIMARY KEY,\n        institution_id TEXT NOT NULL REFERENCES institutions(id) ON DELETE CASCADE,\n        supervisor_id TEXT REFERENCES supervisors(id) ON DELETE SET NULL,\n        source TEXT NOT NULL,\n        rows_total INTEGER NOT NULL,\n        rows_created INTEGER NOT NULL,\n        rows_updated INTEGER NOT NULL,\n        rows_skipped INTEGER NOT NULL,\n        errors_json TEXT,\n        ran_at INTEGER NOT NULL\n      );\n      CREATE INDEX idx_campus_import_institution ON campus_import_runs(institution_id);\n    ",
  "ALTER TABLE learner_projects ADD COLUMN steps_done_json TEXT;",
  "\n      ALTER TABLE aerie_families ADD COLUMN parent_pin_hash TEXT;\n      ALTER TABLE aerie_families ADD COLUMN pin_required INTEGER NOT NULL DEFAULT 0;\n    ",
  "\n      ALTER TABLE aerie_families ADD COLUMN secondary_supervisor_id TEXT REFERENCES supervisors(id) ON DELETE SET NULL;\n      CREATE INDEX idx_aerie_families_secondary ON aerie_families(secondary_supervisor_id);\n    ",
  "ALTER TABLE lessons ADD COLUMN interactive_blocks_json TEXT;",
  "\n      CREATE TABLE learner_memory_new (\n        id TEXT PRIMARY KEY,\n        learner_id TEXT NOT NULL REFERENCES learners(id) ON DELETE CASCADE,\n        memory_type TEXT NOT NULL CHECK (memory_type IN (\n          'topic_covered','topic_mastered','difficulty_observed','misconception','preference','interest',\n          'project_history','reference_anchor','sibling_collaboration','goal_progress',\n          'session_summary','corvus_observation'\n        )),\n        subject TEXT,\n        content TEXT NOT NULL,\n        weight REAL NOT NULL DEFAULT 1.0,\n        created_at INTEGER NOT NULL,\n        last_referenced_at INTEGER\n      );\n      INSERT INTO learner_memory_new SELECT * FROM learner_memory;\n      DROP TABLE learner_memory;\n      ALTER TABLE learner_memory_new RENAME TO learner_memory;\n      CREATE INDEX idx_memory_learner ON learner_memory(learner_id);\n      CREATE INDEX idx_memory_type ON learner_memory(learner_id, memory_type);\n    "
];
